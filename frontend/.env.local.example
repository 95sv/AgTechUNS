# URL del backend FastAPI tal como la alcanza el NAVEGADOR (no la red interna
# de Docker). NEXT_PUBLIC_* se incrusta en el bundle del cliente en build time.
NEXT_PUBLIC_API_URL=http://localhost:8000
